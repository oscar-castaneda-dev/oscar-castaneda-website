import { Inter_Tight, Lora } from "next/font/google";

export const inter = Inter_Tight({
  variable: "--font-inter",
  subsets: ["latin"],
});

export const lora = Lora({
  variable: "--font-lora",
  subsets: ["latin"],
});
